# Activity Reporting System - Environment Variables
# Copy this file to .env and fill in your actual credentials

# Email Configuration (for SMTP delivery)
REPORTPILOT_SMTP_HOST=smtp.gmail.com
REPORTPILOT_SMTP_PORT=587
REPORTPILOT_FROM_EMAIL=your-email@example.com
REPORTPILOT_EMAIL_PASSWORD=your-app-password

# Slack Configuration (choose one method)
# Method 1: Bot Token (recommended for file uploads)
REPORTPILOT_SLACK_BOT_TOKEN=xoxb-your-bot-token

# Method 2: Webhook URL (simpler, no file uploads)
REPORTPILOT_SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# API Data Sources (if using API reader)
# REPORTPILOT_API_KEY=your-api-key-here
# REPORTPILOT_API_TOKEN=your-api-token-here

# Optional: Override config.yaml settings
# REPORTPILOT_OUTPUT_DIRECTORY=custom/output/path
# REPORTPILOT_SCHEDULE_TYPE=cron
